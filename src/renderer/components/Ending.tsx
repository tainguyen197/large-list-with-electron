import { Box } from '@mui/material';

const Ending = () => (
  <Box
    sx={{
      width: '100%',
      textAlign: 'center',
      height: '32px',
      py: 2,
      color: '#0000005e',
    }}
  >
    You have reached your destination.
  </Box>
);

export default Ending;
